# Leave empty or use placeholders — app auto-runs in mock data mode.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: force mock mode (auto-enabled when URL/key are empty or placeholders)
VITE_USE_MOCK_DATA=true
